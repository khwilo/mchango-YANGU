<% @contributions.each do |contribution| %> 
  <%= image_tag contribution.image.url(:medium) %>
  <br>
  <%= contribution.phone %>
  <br>
  <%= contribution.description %>
  <br>
  <%= contribution.amount %>

  <%= simple_form_for contribution do |f| %>
    <%= f.input  :donation, label: 'Donation' %>
    <%= f.button :submit %>
  <% end %>
<% end %>

